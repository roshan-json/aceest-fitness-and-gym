apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-canary
  labels:
    app: aceest-fitness
spec:
  selector:
    app: aceest-fitness
    version: blue  # Initially points to stable version
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP